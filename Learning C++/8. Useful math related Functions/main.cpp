#include <iostream>
#include <cmath>

int main() {
    double x = 3.14;
    double y = 4;
    double z;

    //z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(2, 4);
    // z = sqrt(16);
    // z = abs(-5);
    // z = ceil(3.2);
    // z = floor(3.8);

    std::cout << z;

    return 0;




}